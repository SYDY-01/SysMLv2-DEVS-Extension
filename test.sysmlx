// 验证原生 SysML v2 语法
package TestPackage {
    part def Engine {
        attribute def Thrust;
    }

    // 验证你的 8 元组扩展语法 SP
    domain part def AdvancedAeroPropulsion {
        // 验证预留槽 \Phi 的嵌套能力
        part core : Engine;
        
        attribute def AltitudeLimit;
    }
}