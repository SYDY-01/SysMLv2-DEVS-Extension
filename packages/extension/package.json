{
    "name": "vscode-sysmlv2-extended",
    "description": "The extension specific package",
    "version": "0.0.1",
    "displayName": "sysmlv2-extended",
    "type": "module",
    "engines": {
        "vscode": "^1.67.0"
    },
    "categories": [
        "Programming Languages"
    ],
    "contributes": {
        "languages": [{
            "id": "sys-m-lv-2-extended",
            "aliases": ["SysMLv2Extended", "sys-m-lv-2-extended"],
            "extensions": [".sysmlx"],
            "configuration": "./language-configuration.json"
        }],
        "grammars": [{
            "language": "sys-m-lv-2-extended",
            "scopeName": "source.sysmlv2-extended",
            "path": "syntaxes/sys-m-lv-2-extended.tmLanguage.json"
        }]
    },
    "activationEvents": [
        "onLanguage:sys-m-lv-2-extended"
    ],
    "main": "./out/extension/main.cjs",
    "scripts": {
        "clean": "shx rm -fr *.tsbuildinfo out syntaxes",
        "vscode:prepublish": "npm run build",
        "build:prepare": "shx mkdir -p ./syntaxes/ && shx cp -f ../language/syntaxes/sys-m-lv-2-extended.tmLanguage.json ./syntaxes/sys-m-lv-2-extended.tmLanguage.json",
        "build": "npm run build:prepare && tsc -b tsconfig.json && node esbuild.mjs",
        "build:clean": "npm run clean && npm run build",
        "watch": "npm run build:prepare && concurrently -n tsc,esbuild -c blue,yellow \"tsc -b tsconfig.json --watch\" \"node esbuild.mjs --watch\""
    },
    "dependencies": {
        "sys-m-lv-2-extended-language": "0.0.1",
        "vscode-languageclient": "~9.0.1",
        "vscode-languageserver": "~9.0.1"
    },
    "devDependencies": {
        "@types/vscode": "~1.67.0",
        "concurrently": "~8.2.1",
        "esbuild": "~0.25.5"
    },
    "vsce": {
      "dependencies": false
    }
}
